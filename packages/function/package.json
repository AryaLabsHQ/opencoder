{
  "name": "@opencoder/function",
  "version": "0.0.1",
  "$schema": "https://json.schemastore.org/package.json",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@cloudflare/workers-types": "4.20250522.0",
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "~3.0.7",
    "wrangler": "4.22.0"
  },
  "scripts": {
    "deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
  }
}
